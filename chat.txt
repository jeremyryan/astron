Now that the chat is available as part of the GraphRAG functionality, there should be a way to use it from the UI. Update the right-hand sidebar so that, if the currently selected projection has GraphRAG with a chat provider configured, there are two tabs, one for the resource list and node details, and one for chat. The chat tab should include a chat interface which allows you to send requests and displays replies using the configured chat provider for that projection. Use an existing chat interface if one is available that can easily be incorporated.
